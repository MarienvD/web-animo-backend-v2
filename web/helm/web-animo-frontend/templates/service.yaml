apiVersion: v1
kind: Service
metadata:
  name: web-animo-frontend
spec:
  type: ClusterIP
  selector:
    app: webanimo-angular
  ports:
    - name: http
      port: 8081
      targetPort: 8081
      protocol: TCP