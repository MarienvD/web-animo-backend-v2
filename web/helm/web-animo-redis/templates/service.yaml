apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-animo-redis.fullname" . }}
  labels:
    {{- include "web-animo-redis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web-animo-redis.selectorLabels" . | nindent 4 }}
