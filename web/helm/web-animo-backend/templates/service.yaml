apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-animo-backend.fullname" . }}
  labels:
    {{- include "web-animo-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web-animo-backend.selectorLabels" . | nindent 4 }}
