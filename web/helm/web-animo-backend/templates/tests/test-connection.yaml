apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "web-animo-backend.fullname" . }}-test-connection"
  labels:
    {{- include "web-animo-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "web-animo-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
