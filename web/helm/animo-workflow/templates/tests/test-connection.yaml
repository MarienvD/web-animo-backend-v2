apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "animo-workflow.fullname" . }}-test-connection"
  labels:
    {{- include "animo-workflow.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "animo-workflow.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
